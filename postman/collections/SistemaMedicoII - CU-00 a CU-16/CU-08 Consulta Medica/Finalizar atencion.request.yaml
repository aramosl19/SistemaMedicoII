$kind: http-request
url: "{{base_url}}/api/medico/citas/{{cita_id}}/finalizar-atencion"
method: POST
body:
  type: json
  content: "{}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 2xx', function () {
          pm.expect(pm.response.code).to.be.oneOf([200,201]);
      });
    language: text/javascript
order: 4000
