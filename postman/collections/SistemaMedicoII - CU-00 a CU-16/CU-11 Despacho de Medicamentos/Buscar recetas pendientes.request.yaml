$kind: http-request
url: "{{base_url}}/api/farmacia/recetas/buscar?recetaId={{receta_id}}"
method: GET
queryParams:
  recetaId: "{{receta_id}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 2xx', function () {
          pm.expect(pm.response.code).to.be.oneOf([200,201]);
      });
    language: text/javascript
order: 1000
