$kind: http-request
url: "{{base_url}}/api/recepcion/citas/buscar?numeroCita={{cita_id}}"
method: GET
queryParams:
  numeroCita: "{{cita_id}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 2xx', function () {
          pm.expect(pm.response.code).to.be.oneOf([200,201]);
      });
    language: text/javascript
order: 1000
