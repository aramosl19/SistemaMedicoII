$kind: http-request
url: "{{base_url}}/api/recepcion/citas/{{cita_id}}/llegada"
method: POST
body:
  type: json
  content: |-
    {
      "emergencia": false
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 2xx', function () {
          pm.expect(pm.response.code).to.be.oneOf([200,201]);
      });
    language: text/javascript
order: 3000
